# Build products
fsh-generated/
output/
dist/
temp/
template/
input-cache/
input/resources/*.json
input/includes/sample-viewer-links.md

# Generated examples/viewer artifacts are build products. GitHub Actions copies
# them into the ephemeral IG/static-site build; they are not committed.

# Tooling deps (restored with `bun install`)
node_modules/

# Local editor and OS files
.DS_Store
.vscode/
.idea/

# IG Publisher indexes template package sources in place
.index.db
.index.json

# site-gen build products
site-gen/out/
site-gen/package.db

site-gen/.shots/

# site-gen build products / working DBs (regenerated; never commit)
temp/site-gen/
site-gen/site.db
site-gen/site.db-wal
site-gen/site.db-shm
site-gen/fixtures/*.db
site-gen/fixtures/*.db-wal
site-gen/fixtures/*.db-shm
!site-gen/fixtures/package.db
